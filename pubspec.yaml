name: little_light_v01
description: "Charity, Donation application.."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  mobx: ^2.0.4
  flutter_mobx: ^2.0.2
  google_fonts: ^6.2.1
  video_player: ^2.4.8

dev_dependencies:
  build_runner: ^2.1.7
  mobx_codegen: ^2.0.4

  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/